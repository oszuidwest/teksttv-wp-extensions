---
name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  TEKSTTV_COMMIT: d1e909e179ae2699a9dc306ab64c00848ee3168c
  STREEKOMROEP_COMMIT: c25bdeb3664cd5557d1c90402ccbdc632060a582

jobs:
  php:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer:v2

      - name: Validate PHP syntax
        run: >-
          find . -path './vendor' -prune -o -path './upstream' -prune -o
          -name '*.php' -type f -print0 | xargs -0 -n1 php --syntax-check

      - name: Validate Composer metadata
        run: composer validate --strict --no-check-publish

      - name: Install dependencies
        run: composer install --no-progress --prefer-dist --no-interaction --optimize-autoloader

      - name: Audit Composer dependencies
        run: composer security

      - name: PHP_CodeSniffer
        run: composer check

      - name: PHPStan
        run: composer analyse

      - name: PHPUnit
        run: composer test -- --no-coverage

      - name: Check out the TekstTV dependency
        if: matrix.php == '8.4'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: oszuidwest/teksttv-wp-plugin
          ref: ${{ env.TEKSTTV_COMMIT }}
          path: upstream/teksttv

      - name: Install the TekstTV runtime dependencies
        if: matrix.php == '8.4'
        run: >-
          composer install --working-dir=upstream/teksttv --no-dev --no-progress
          --prefer-dist --no-interaction --optimize-autoloader

      - name: Configure the Plugin Check environment
        if: matrix.php == '8.4'
        run: |
          cat > .wp-env.json <<'JSON'
          {
            "core": null,
            "port": 8880,
            "testsEnvironment": false,
            "mappings": {
              "wp-content/plugins/teksttv-wp-extensions": ".",
              "wp-content/plugins/teksttv": "./upstream/teksttv"
            }
          }
          JSON

      - name: Set up Node for Plugin Check
        if: matrix.php == '8.4'
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6.0.0
        with:
          node-version: '24'

      - name: Install Plugin Check tooling
        if: matrix.php == '8.4'
        run: npm install --global @wordpress/env@11.10.0

      - name: WordPress Plugin Check
        if: matrix.php == '8.4'
        run: |
          wp-env start --update
          wp-env run cli wp plugin install plugin-check --version=2.0.0 --activate
          wp-env run cli wp plugin activate teksttv
          wp-env run cli wp plugin activate teksttv-wp-extensions
          wp-env run cli wp plugin check teksttv-wp-extensions \
            --slug=teksttv-wp-extensions \
            --exclude-files=scripts/check-coverage.php \
            --exclude-directories=stubs,tests,upstream,vendor \
            --exclude-checks=file_type,i18n_usage,plugin_readme,plugin_updater

      - name: Stop the Plugin Check environment
        if: ${{ always() && matrix.php == '8.4' }}
        run: wp-env stop

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.4'
          coverage: xdebug
          tools: composer:v2

      - run: composer install --no-progress --prefer-dist --no-interaction
      - run: composer test -- --coverage-clover=coverage.xml
      - run: php scripts/check-coverage.php coverage.xml 90

      - name: Upload coverage report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.0
        with:
          name: php-coverage
          path: coverage.xml
          retention-days: 14
          if-no-files-found: error

  contract:
    name: Upstream contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Check out TekstTV extension API
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: oszuidwest/teksttv-wp-plugin
          ref: ${{ env.TEKSTTV_COMMIT }}
          path: upstream/teksttv

      - name: Check out Streekomroep schedule API
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: oszuidwest/streekomroep-wp
          ref: ${{ env.STREEKOMROEP_COMMIT }}
          path: upstream/streekomroep

      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2

      - run: composer install --no-progress --prefer-dist --no-interaction
      - run: composer contract
        env:
          TEKSTTV_PATH: upstream/teksttv
          STREEKOMROEP_PATH: upstream/streekomroep
